node_modules
/.cache
/build
/dist
/public/build
/.mf
.env
.shopify
.react-router

# Nano Banana MCP companion thumbnails (not used by the app)
public/**/*_thumb.jpeg

# Local logs / scratch
error.log
server.log
ssr-test.js

# Venue image fetch audit (optional: commit for team visibility)
reports/*.json

# Nested clone � track via submodule if needed
